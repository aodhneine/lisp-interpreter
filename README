# lisp-interpreter - simple lisp interpreter in OCaml

This is an intepreter for a minimal subset of lisp. To build it you need `make`
and `ocamlopt.opt`.

## License

`lisp-interpreter` is free and unencumbered software released into the public
domain. For more information refer to the [COPYING] file.

[COPYING]:  ./COPYING
